<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

/*
 * Routen des Moduls Sync. Wird ausschließlich vom SyncServiceProvider geladen.
 */
Route::prefix('')->group(static function (): void {
    //
});
