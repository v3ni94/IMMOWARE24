<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

/*
 * Routen des Moduls Connector. Wird ausschließlich vom ConnectorServiceProvider geladen.
 */
Route::prefix('')->group(static function (): void {
    //
});
