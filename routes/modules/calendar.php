<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

/*
 * Routen des Moduls Calendar. Wird ausschließlich vom CalendarServiceProvider geladen.
 */
Route::prefix('')->group(static function (): void {
    //
});
