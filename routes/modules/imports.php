<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

/*
 * Routen des Moduls Imports. Wird ausschließlich vom ImportsServiceProvider geladen.
 */
Route::prefix('')->group(static function (): void {
    //
});
