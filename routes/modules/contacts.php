<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

/*
 * Routen des Moduls Contacts. Wird ausschließlich vom ContactsServiceProvider geladen.
 */
Route::prefix('')->group(static function (): void {
    //
});
