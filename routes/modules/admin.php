<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

/*
 * Routen des Moduls Admin. Wird ausschließlich vom AdminServiceProvider geladen.
 */
Route::prefix('')->group(static function (): void {
    //
});
