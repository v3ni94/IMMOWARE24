<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

/*
 * Routen des Moduls Documents. Wird ausschließlich vom DocumentsServiceProvider geladen.
 */
Route::prefix('')->group(static function (): void {
    //
});
