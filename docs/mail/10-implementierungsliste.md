# 10 Implementierungsliste

Stand: 12.09.2026 nach Integrationslauf, Security-Review, Fix-Lauf, Abschlusslauf 2 und Abschlusslauf 3 (alle offenen Punkte aus 08 Abschnitt 11.2 abgearbeitet) (Module Mail, Gmail, Cases, Sla, Actions, Lexware, Ai, Drive, MailUi, MailIntegration; Findings und Status in 08 Abschnitt 11). Hinweis: Der Auftragstext mit den Abschnitten 2 bis 22 lag nicht als Datei vor; die Zuordnung der Nummern ist rekonstruiert. Statusspalte je Anforderung mit den Stufen: **implementiert** (Code vorhanden), **automatisiert getestet** (PHPUnit mit Fakes und `Http::fake`), **manuell getestet** (in dieser Umgebung nur Artisan-Befehle: hub:doctor, schedule:list, route:list, migrate:fresh), **mit echter Integration geprüft** (hier immer nein: keine Zugangsdaten, Hosts gesperrt), **begrenzt wegen fehlender Fähigkeit** (Fremdsystem bietet die Funktion nicht oder sie ist nicht belegt), **offen**.

| Nr. | Abschnitt (rekonstruiert) | Anforderung | Umsetzung (geplant) | Konzeptbezug | Status |
|---|---|---|---|---|---|
| 2.1 | Grundsätze | Gelesen ist nicht bearbeitet | Statusdimension Bearbeitung unabhängig vom Öffnen; `is_read_in_gmail` informativ | 04 §1.1 | implementiert, automatisiert getestet (HistorySyncTest, MailEndToEnd), echte Integration nein |
| 2.2 | Grundsätze | Beantwortet ist nicht erledigt | Kommunikationsdimension getrennt, `resolved` nur mit Abschlussbedingungen | 04 §1.2, §2 | implementiert, automatisiert getestet (GmailReplyTest, CaseClosingTest, CaseWorkflowTest), echte Integration nein |
| 2.3 | Grundsätze | HTTP-Erfolg ist kein Geschäftsergebnis | `http_ok_unverified`, `mail_verifications`, Versandabgleich | 04 §1.3, 02 §5 | implementiert, automatisiert getestet (StalePlanAndPartialExecutionTest, SendServiceTest), echte Integration nein |
| 2.4 | Grundsätze | Offener Vorgang hat Verantwortlichen, nächsten Schritt, Fälligkeit | Pflichtfelder ab `assigned`, Liste "Unvollständig" | 04 §1.1, §9 | implementiert, automatisiert getestet (SlaClockTest, MailUiPagesTest); Anzeige und Ampel statt harter Sperre; echte Integration nein |
| 2.5 | Grundsätze | Technische Fehler nie als Erfolg | `failed` bei Exception, DLQ, kein Pfad zu `verified` | 04 §1.3, 06 Nr. 13 | implementiert, automatisiert getestet (TimeoutAndScheduleTest, EmergencyEscalationTest), echte Integration nein |
| 3.1 | Systemrollen | Gmail Mailsystem, Immoware24 Fachsystem, Lexware Rechnungsprogramm, Drive Dokumentenquelle | Modulschnitt, Capability-Matrix | 01 §7, 03 | implementiert, automatisiert getestet (Modulschnitt, Adapterregistry), echte Integration nein |
| 3.2 | Systemrollen | Nicht konfigurierte Integrationen sichtbar "Nicht eingerichtet" | `IntegrationStatusService`, Status je Postfach und Integration | 01 §8, 03 | implementiert, automatisiert getestet (FakeProviderBindingTest, Integrationsseite), manuell getestet (hub:doctor zeigt Flags und Integrationsstatus), echte Integration nein |
| 4.1 | Architektur | Additive Erweiterung derselben Anwendung, Domain mail.muellerhv.de | `Route::domain`, Middleware `mail.domain` | 01 §1 bis §3 | implementiert, automatisiert getestet (MailDomainRoutingTest), echte Integration nein (DNS und TLS nicht in dieser Umgebung) |
| 4.2 | Architektur | Hostgebundene Sitzungen | `SESSION_DOMAIN` leer, Test | 01 §4, 06 Nr. 5 | implementiert, automatisiert getestet (Cookie ohne Domain), Ablehnung eines Fremdhost-Cookies nur manuell prüfbar, echte Integration nein |
| 4.3 | Architektur | Middleware-Gruppen `mail`, `mail.push`, `mail.fresh` | `MailServiceProvider` | 01 §5 | implementiert, automatisiert getestet (mail_middleware_groups_are_registered), echte Integration nein |
| 4.4 | Architektur | Queues `mail-high`, `mail-sync`, `mail-ai` | Config, Worker-Kommandos, compose | 01 §6, 09 §4 | implementiert (config, compose.yaml mail-worker-high und mail-worker, deploy/supervisor, deploy/systemd), automatisiert getestet (Queue-Namen der Jobs), manuell getestet (schedule:list), echte Integration nein (kein Redis in dieser Umgebung) |
| 4.5 | Architektur | Feature-Flags (6) Default false, Staging-Schutz | `config/hub/mail.php`, `MailBootGuard` | 01 §8, §9 | implementiert, automatisiert getestet (MailBootGuardTest, FakeProviderBindingTest), echte Integration nein |
| 5 | Datenmodell | Alle `mail_`-Tabellen mit Indizes, Unique, FKs | Migrationen SQLite und MariaDB | 02 | implementiert (19 additive Migrationen, neu Legal Hold, `open_key` Unique, `payload_json` longText, `approval_reauth_confirmed_at`), automatisiert getestet (MailMigrationsTest), manuell getestet (migrate:fresh SQLite, 47 Migrationen), MariaDB-Lauf offen (nur CI-Job tests-mariadb) |
| 6.1 | Kern | Teams, Mitglieder, Postfächer, Aliasse, Postfachrechte | Modul Mail: Modelle, Services; Modul MailUi: Administration (Teams, Rollenmatrix, Postfächer, Aliasse, Postfachrechte, Objektzuständigkeiten, Arbeitszeiten, Feiertage, SLA-Regeln, Eskalation, Bereitschaft, KI-Kosten, Aufbewahrung) und Einrichtungsassistent | 02 §1, 05 §4 | implementiert, automatisiert getestet (AdminMailboxesTest, SetupWizardTest), echte Integration nein |
| 6.2 | Kern | Gesellschaftszuordnung je Postfach und Alias, keine Vermischung | `legal_entity_code`, Prüfung beim Entwurf | 02 §1, 07 §4 | implementiert, automatisiert getestet (SendServiceTest alias_of_other_entity, DraftServiceTest), echte Integration nein |
| 7.1 | Gmail | OAuth2 Authorization Code mit Refresh, Token verschlüsselt, Widerruf | `GmailOAuthService` | 07 §6 | implementiert, automatisiert getestet (OAuthFlowTest), mit echter Integration geprüft nein (kein Google-Client) |
| 7.2 | Gmail | Minimale Scopes je Flag, Scope-Prüfung nach Refresh | Scope-Matrix im Code | 07 §2, §3 | implementiert (Scope-Matrix in config/hub/gmail.php), automatisiert getestet (OAuthFlowTest::test_authorization_url_carries_state_pkce_and_minimal_scopes), Scope-Prüfung nach Refresh begrenzt (nur Snippet-Wissen), echte Integration nein |
| 7.3 | Gmail | users.watch mit täglicher Erneuerung, Watch-Status erst nach Bestätigung aktiv | `WatchRenewJob`, `mail_sync_states.watch_status` | 07 §7, 06 Nr. 8 | implementiert, automatisiert getestet (WatchRenewTest, PushEndpointTest), zentral eingeplant in routes/console.php, echte Integration nein |
| 7.4 | Gmail | Pub/Sub-Push mit JWT-Prüfung, Pfad-Token, Dedup | `PushController`, `mail_push_events`, JWKS-Cache | 02 §2, 08 §9 | implementiert, automatisiert getestet (PushEndpointTest, GoogleIdTokenVerifierTest), echte Integration nein; Abweichung: Pfad /mail/gmail/push statt /api/mail/gmail/push |
| 7.5 | Gmail | history.list inkrementell, 404 zu Full-Sync, Polling-Fallback | `GmailSyncService`, `mail_sync_states` | 06 Nr. 6 | implementiert, automatisiert getestet (HistorySyncTest), echte Integration nein; Polling-Fallback ist der Reconcile-Lauf alle 30 Minuten |
| 7.6 | Gmail | messages.get metadata, Body bei Bedarf, Anhänge nach Allowlist | `MessageImporter`, `mail_messages`, `mail_attachments` | 02 §2, 08 §3 | implementiert, automatisiert getestet (MimeParserTest, HistorySyncTest), begrenzt: Anhangsinhalte werden nicht persistiert (nur Metadaten, Hash, Allowlist-Status), echte Integration nein |
| 7.7 | Gmail | MIME-Parser und MIME-Builder ohne Pakete | `MimeParser`, `MimeBuilder` | 06 Ergänzung | implementiert, automatisiert getestet (MimeParserTest, MimeBuilderTest, HtmlSanitizerTest) |
| 7.8 | Gmail | Drafts anlegen/ändern mit Threading (In-Reply-To, References, threadId) | `GmailDraftService` | 02 §5 `mail_drafts` | implementiert, automatisiert getestet (DraftServiceTest), über MailIntegration mit der Oberfläche verdrahtet (nur bei MAIL_GMAIL_DRAFTS_ENABLED), echte Integration nein |
| 7.9 | Gmail | Versand nur mit Flag, Recht, Freigabe, 2fa.fresh; Versandabgleich | `SendAction`, `SendReconciliationService` | 07 §4, 06 Nr. 12 | implementiert, automatisiert getestet (SendServiceTest, CaseWorkflowTest), Oberfläche meldet ok erst nach verifiziertem Abgleich, echte Integration nein |
| 7.10 | Gmail | sendAs-Aliasse lesen, nur akzeptierte anbieten | `AliasSyncJob` | 02 §1 | implementiert (`AliasSyncJob` täglich 03:45 über sendAs.list, nur akzeptierte Aliasse und Primäradresse, nicht mehr gelieferte auf `missing`, kein Hard Delete), automatisiert getestet (GmailProviderHttpTest, AliasSyncTest), echte Integration nein |
| 7.11 | Gmail | Quota-Budget und 429-Backoff | `GmailQuotaBudget` mit `RateLimitManager` | research §8 | implementiert (QuotaCounter, 429-Backoff), automatisiert getestet (GmailProviderHttpTest), Quota-Kosten aus Snippets, echte Integration nein |
| 7.12 | Gmail | Altmails behalten Empfangszeit, Uhren als Altbestand | `LegacyImportPolicy` | 04 §8, 06 Nr. 19 | implementiert (Empfangszeit aus internalDate, Altbestandsregel), automatisiert getestet (SlaClockTest inkl. legacy_import_creates_cancelled_clocks, PriorityClassifierTest); `LegacyImportPolicy` legt Uhren als cancelled an, Fälligkeit bleibt Pflichtfeld; Import eines 2025-Bestands über FakeGmail offen |
| 8.1 | Vorgänge | Vorgang, Teilanliegen, Nachrichtenverknüpfung, externe Referenzen | Modul Cases | 02 §3 | implementiert, automatisiert getestet (CaseOpeningTest, MailEndToEnd: Vorgang mit zwei Teilanliegen aus Push), echte Integration nein |
| 8.2 | Vorgänge | Drei Statusdimensionen mit Übergangsprüfung | `CaseStateMachine` | 04 §1 | implementiert, automatisiert getestet (StateMachinesTest) |
| 8.3 | Vorgänge | Abschlussbedingungen je Vorgangstyp | `ClosureConditions` | 04 §2 | implementiert (Startvorschläge in config/hub/cases.php), automatisiert getestet (CaseClosingTest), Bestätigung der Bedingungen durch Geschäftsführung offen |
| 8.4 | Vorgänge | Zuordnung Kontakt/Objekt/Vertrag nur über Kennung oder Mensch, KI nur Vorschlag | `CaseAssignmentService`, `mail_assignment_decisions` | 02 §3, 03 §5 | implementiert, automatisiert getestet (AssignmentTest), über MailIntegration an die Oberfläche verdrahtet (Kandidaten aus Kennungen, Bestätigung nur per ID) |
| 8.5 | Vorgänge | Aufgaben mit Alt/Neu, "Manuell bestätigt", zweite Bestätigung bei Bankdaten | `TaskService`, `mail_tasks` | 03 §1, 05 §5 | implementiert, automatisiert getestet (BankChangePolicyTest, IbanChangeFlowTest), Bestätigung über Oberfläche mit Reauth; zweite Bestätigung bei Bankdaten nur über die zwei Freigaben, nicht bei der Aufgabenbestätigung selbst |
| 9.1 | SLA | Prioritäten P0 bis P3 mit Regeln, KI-Herabstufung nie automatisch | `PriorityRules` | 04 §3 | implementiert, automatisiert getestet (PriorityClassifierTest, BusinessRuleValidatorTest) |
| 9.2 | SLA | Vier Uhren, zwei Ampeln | `SlaClockService`, `mail_sla_clocks` | 04 §4, §5 | implementiert, automatisiert getestet (SlaClockTest) |
| 9.3 | SLA | Startwerte P0 10 min/5 min Eskalation, P1 4 Arbeitsstunden, P2 2 Arbeitstage, P3 4 Arbeitstage, gelb 50 % | Seed `mail_sla_rules` | 04 §6 | implementiert (config/hub/sla.php), automatisiert getestet, Bestätigung der Startwerte durch Geschäftsführung offen |
| 9.4 | SLA | Arbeitskalender Mo bis Fr 08:00 bis 16:30, Feiertage NRW konfigurierbar, Sommerzeit | `WorkCalendar`, `mail_work_calendars`, `mail_holidays` | 04 §7, 06 Nr. 18 | implementiert, automatisiert getestet (BusinessTimeTest, HolidayProviderTest); Kalender aus mail_work_calendars vorbereitet, Provider bindet Config |
| 9.5 | SLA | Eskalation und Notfallqueue | `EscalationService`, Queue `mail-high` | 04 §6, 06 Nr. 17 | implementiert, automatisiert getestet (EmergencyEscalationTest, MailEndToEnd), begrenzt: Kanäle sms und call nicht eingerichtet, Bereitschaft nur über MAIL_ON_CALL_USER_IDS, echte Integration nein |
| 10.1 | Aktionen | Action Plans mit Versionen und Hash | Modul Actions | 02 §5 | implementiert, automatisiert getestet (DiffHasherTest, StalePlanAndPartialExecutionTest) |
| 10.2 | Aktionen | Freigaben Vier-Augen, Autor ungleich Freigeber, Re-Auth | `ApprovalService` | 05 §5, 06 Nr. 16 | implementiert, automatisiert getestet (ApprovalCenterTest, LexwareMultiAddressTest), Identitätsprüfung und Retry über Oberfläche, Freigabe an ActionPolicy verdrahtet |
| 10.3 | Aktionen | Ausführung mit Idempotenz, Allowlist | `ActionExecutor`, `ActionAllowlist`, `mail_executions` | 03 §6 | implementiert, automatisiert getestet (BankChangePolicyTest Allowlist, ExecuteActionJob idempotency_key) |
| 10.4 | Aktionen | Verifikation je Zielsystem | `VerificationService`, `mail_verifications` | 04 §1.3 | implementiert, automatisiert getestet; Ereignis ExecutionVerified schreibt Aufgaben- und Kommunikationsstatus des Vorgangs fort (MailEndToEnd) |
| 10.5 | Aktionen | Outbox in derselben Transaktion | `mail_outbox` | 02 §5 | implementiert (ActionOutboxWriter in derselben Transaktion, `ActionOutboxDispatcher` und `DispatchActionOutboxJob` minütlich als Verarbeiter), automatisiert getestet (ReviewFindingsTest outbox_entries_are_dispatched); Rollback-Test (OutboxTransactionTest) offen |
| 11 | Immoware24 | Nur öffentliche Services nutzen; Adresse ändern nicht verfügbar; Posteingang-Upload aus dem Mail-Modul | Adapter auf `CapabilityRegistry`, `ProposedChangeService`; `PosteingangUploadService` wird aus dem Mail-Modul nicht aufgerufen | 00 §2, 03 §1 | teilweise implementiert: Adresse und Bankdaten in Immoware24 nur als manuelle Aufgabe (StalePlanAndPartialExecutionTest, AddressAndWaterDamageFlowTest). Posteingang-Upload als Aktion des Mail-Moduls **nicht implementiert, offen**: `ImmowareTargetAdapter` erzeugt nur manuelle Aufgaben, `ActionAllowlist::TYPES` kennt keinen Upload-Typ, der Key `immoware.posteingang.upload` in config/hub/actions.php hat keine Adapterbindung. Der bestehende Hub-Schreibpfad (write_operations) bleibt unberührt. |
| 12 | Lexware | Adapter Kontakt lesen, GET-Version-PUT-GET, Adressanzahl-Prüfung, 409 als Konflikt, 1 rps | Modul Lexware | 03 §3 | implementiert, automatisiert getestet (LexwareClientTest, LexwareMultiAddressTest), Endpunkte und Pagination aus Snippets, echte Integration nein |
| 13 | KI | OpenAI-Adapter Responses API `store: false`, Schema-Validierung, Maskierung, Injection-Schutz, nur Vorschläge | Modul Ai | 08 §4, 03 §5 | implementiert, automatisiert getestet (OpenAiProviderTest, AiSuggestionServiceTest), Klassifikation als Job auf mail-ai nur bei MAIL_AI_ENABLED (MailIntegration), Feldnamen aus Snippets, echte Integration nein |
| 14 | Drive | Lesender Adapter, Referenzen, Berechtigungsanzeige | Modul Drive | 03 §4 | implementiert, automatisiert getestet (DriveProviderTest, DriveAccessTest, DriveOAuthFlowTest: PKCE, nur `drive.readonly`, Widerruf, Status auf Integrationsseite), begrenzt: OCR nicht eingerichtet, echte Integration nein |
| 15 | Oberfläche | Blade unter resources/views/mail, Dashboard, Listen, Detail, Ampeln, "Nicht eingerichtet", Staging-Banner | Modul MailUi: Dashboard, Team-Inbox (Filter, Sortierung, Sammelaktion nur Zuordnung/Kategorie/Aufgabe), Meine Arbeit, Vorgangsdetail dreispaltig, Freigabecenter (einzeln, Reauth, IBAN maskiert), Integrationen, Tastaturkürzel j/k/a/e/?, mobile Ansicht. Fachlogik über Contracts in app/Modules/MailUi/Contracts mit Null-Implementierungen, Verdrahtung durch Integrationsagent offen | 01 §7, 04 §9 | implementiert, automatisiert getestet (MailUi-Tests, MailEndToEnd über HTTP), Verdrahtung durch Modul MailIntegration erledigt; Branding offen |
| 16 | Rollen | Permission-Katalog mail.*, Mail-Rollen als Bündel, Postfach-/Objekt-/Bankdaten-/Freigabe-/Versand-/Export-/Integrationsrechte | Merge in Security-Config, Policies | 05 | implementiert, automatisiert getestet (MailAccessTest, MailUiPagesTest) |
| 17 | Sicherheit | 2FA Pflicht, 2fa.fresh für kritische Aktionen, Egress-Allowlist, CSP, HTML-Bereinigung | Middleware, Config | 05 §6, 08 §9 | implementiert (2fa, 2fa.fresh mit Prüfung im ApprovalController und DraftController sowie auf Service-Ebene in DraftService::approve und SendService::send, HtmlSanitizer, CSP aus der Anwendung über `MailSecurityHeaders` plus nginx, Host-Guard für Hub-Routen auf dem Mail-Host, Vier-Augen beim Versand serverseitig), automatisiert getestet (ApprovalCenterTest, HtmlSanitizerTest, MailHostGuardTest, DraftFourEyesTest, DraftReauthTest), Egress-Allowlist nur als Betriebsvorgabe, echte Integration nein |
| 18 | Datenschutz | AVV-Liste, Datenflüsse, Minimierung, Maskierung, Retention, Auskunft, Logging | Dokument 08, `mail:retention:apply`, Auskunftsexport | 08 | implementiert (Maskierung, Verschlüsselung inklusive `payload_json`, `mail:retention:apply` mit Legal Hold und Dry-Run-Standard, Auskunftsexport `mail:subject-access-export` und Admin-Seite, Dokument 08), automatisiert getestet (PromptMaskerTest, BankDataMaskerTest, RetentionApplyTest, SubjectAccessExportTest, AiSuggestionEncryptionTest); Fristen der Aufbewahrung durch Geschäftsführung zu bestätigen |
| 19 | Audit | Alle Pflichtereignisse über `AuditLoggerInterface`, neue AuditSource-Fälle additiv | `AuditSource::Mail`, `GmailPush`, `Ai` | 02 §7, 05 §8 | implementiert (AuditSource::Mail additiv, AuditLoggerInterface), automatisiert getestet (ApprovalCenterTest Audit, AdminMailboxesTest); vollständiger Ereigniskatalog-Test offen |
| 20 | Deployment | DNS A/AAAA, TLS, nginx-Block, keine MX/SPF/DKIM/DMARC-Änderung, Staging/Produktion, Worker, Zeitpläne | Dateien unter deploy/, docker/, compose | 09 | implementiert (deploy/nginx/mail.muellerhv.de.conf, docker/nginx/mail.muellerhv.de.conf, compose.yaml Mail-Worker, deploy/supervisor, deploy/systemd, .env.example, routes/console.php), manuell getestet (schedule:list, hub:doctor), mit echter Integration geprüft nein (DNS, TLS, Redis nicht in dieser Umgebung) |
| 21 | Tests | 20 Abnahmefälle plus Ergänzungen mit Http::fake; Mock ist kein Live-Test | tests/Feature/Mail*, tests/Unit/Mail* | 06 | implementiert, automatisiert getestet: 896 Tests, 888 bestanden, 8 übersprungen, 6.781 Assertions (Abschlusslauf 2 am 13.09.2026, davon 321 Tests der Mail-Module); Pint und PHPStan Level 5 ohne Befund; Status je Abnahmefall in 06; Mock ist kein Live-Test |
| 22 | Dokumentation und Abnahme | README-Abschnitt "Mail-Modul", Testbericht, Freigaben der Geschäftsführung je Flag dokumentiert | README, `docs/mail/11-testbericht.md` | alle | implementiert (README Abschnitt Mail-Modul, docs/immoware/10-test-report.md Abschnitt 6, diese Liste); Freigaben der Geschäftsführung je Flag offen |

## Nachtrag: KI-Anbieterauswahl, Lernphase Immoware24, Prozessdatenbank (21.09.2026)

Auftrag der Geschäftsführung: KI-Einsatz mit OpenAI und Anthropic (OpenAI bevorzugt, Kostenentscheidung), eine
wiederholbare Lernphase für die Immoware24-Struktur sowie eine Prozessdatenbank, die aus abgeschlossenen
Vorgängen lernt und ähnliche neue Vorgänge schneller und mit bekanntem Vorgehen bearbeitet.

- **Anbieterauswahl (Modul Ai)**: `AnthropicProvider` (Messages API, erzwungener Tool-Aufruf für strukturierte
  Ausgaben) neben dem bestehenden `OpenAiProvider`; `SelectingAiProvider` wählt anhand `hub.ai.provider_priority`
  (Standard `openai,anthropic`) und weicht bei Nichteinrichtung oder Nichterreichbarkeit automatisch aus.
  Kostenschätzung jetzt je Anbieter. `mail_ai_runs.provider` zeigt den tatsächlich antwortenden Anbieter.
  Implementiert, getestet (Http::fake, kein Live-Test möglich).
- **Lernphase Immoware24 (neues Modul Learning, kein Bezug zu Vorgängen)**: erkundet lesend über die belegten
  Zugangswege (WebDAV, CardDAV, CalDAV, bereits erfasste Dateiexporte) die aktuelle Struktur, tiefer und breiter
  als der laufende Sync, vergleicht mit dem vorigen Lauf und kann eine KI-Auswertung anstoßen
  (`learning_document_rules`, `learning_field_mapping`, `learning_contact_mapping`, `learning_calendar_mapping`
  als neue Aufgaben des Moduls Ai, Ergebnis stets ein Vorschlag in `mail_ai_suggestions`). Schreibt nie nach
  Immoware24 und ändert keine Konfigurationsdatei automatisch; eine Übernahme bleibt ein manueller Schritt.
  Konsole `hub:immoware:learn`, Admin-Seite (Recht `learning.manage`, Tabelle `learning_runs`). Implementiert,
  30 Tests.
- **Prozessdatenbank (neues Modul Playbooks)**: `mail_playbooks` fasst zusammen, wie Vorgänge einer Kategorie
  bisher bearbeitet wurden (Schrittfolge aus den erlaubten Aktionsarten, `hub.ai.allowed_action_types`).
  `PlaybookMatchService` vergleicht einen neuen Vorgang zuerst regelbasiert und kostenlos mit den aktiven
  Vorlagen (Stichwortähnlichkeit von Titel und Kategorie); nur bei unklarem, aber nicht aussichtslosem Ergebnis
  (`hub.playbooks.match.low_threshold` bis `high_threshold`) wird die KI zur Einschätzung und für Abweichungen
  hinzugezogen (neue Aufgaben `playbook_match`, `playbook_draft_steps`). Jeder Abgleich wird protokolliert
  (`mail_playbook_matches`). `PlaybookLearningService` schlägt beim Abschluss eines Vorgangs ohne passende oder
  abgelehnte Vorlage eine neue Prozessvorlage aus dem tatsächlichen Verlauf vor (Aufgaben, Statuswechsel der
  Bearbeitungsdimension) und bei vermerkten Abweichungen eine verbesserte Fassung der genutzten Vorlage. Jeder
  Vorschlag bleibt ein Entwurf, bis eine Person ihn prüft und aktiviert (`PlaybookService`, nur eine aktive
  Vorlage je Kategorie und Organisation). Ereignisse `CaseOpened` (Abgleich) und `CaseStatusChanged`
  (Dimension processing, Zielstatus closed; Lernen) lösen je einen Job auf der Queue `mail-ai` aus, beide hinter
  `hub.playbooks.flags.enabled` beziehungsweise zusätzlich `.flags.ai`. Konsole `hub:playbooks:relearn` baut die
  Datenbank rückwirkend aus bereits abgeschlossenen Vorgängen auf. Verwaltung unter mail.muellerhv.de,
  Administration, Reiter Prozessdatenbank (Recht `mail.admin`, wie die übrigen Administrationsbereiche).
  Implementiert, 27 Tests.
- Offen: Die Prozessdatenbank ist noch nicht in die Vorgangsansicht selbst eingeblendet (kein Hinweis "passende
  Vorlage gefunden" direkt im Vorgang); die Entscheidung über einen Abgleich erfolgt bislang ausschließlich über
  die eigene Seite Offene Abgleiche. Eine engere Verzahnung mit `CaseController`/der Vorgangsansicht ist ein
  sinnvoller nächster Schritt, sobald gewünscht.

## Verdrahtung (Modul MailIntegration, 12.09.2026)

`app/Modules/MailIntegration` bindet die vier Verträge der Oberfläche (`CaseCommandInterface`, `DraftWorkflowInterface`, `ApprovalWorkflowInterface`, `CandidateResolverInterface`) an CaseService, DelegationService, TaskService, LockService, DraftService, SendService, ApprovalService, ActionPolicy und AssignmentService. Ereignisketten: Gmail `MessageImported` erzeugt Vorgang mit regelbasierten Teilanliegen (`CaseIntakeService`, config `hub.cases.intake`) oder schreibt bestehende Vorgänge des Threads fort; Gmail `GmailReplyDetected` erledigt den Kommunikationsbedarf (Modul Cases); Gmail `SendVerificationCompleted` setzt gesendet erst nach verifiziertem Abgleich; Cases `CaseOpened` startet `AiClassificationJob` auf mail-ai nur bei `MAIL_AI_ENABLED`; die P0-Regel löst die EmergencyQueue (mail-high) im CaseService aus; Actions `ExecutionVerified` schreibt Aufgabenstatus, Geschäftsstatus und einen Kommunikationsvorschlag (`ReplyProposalService`, lokaler Entwurf nur mit verifizierten Änderungen). Zeitpläne zentral in `routes/console.php`. Neue Oberflächenrouten: Aufgabe manuell bestätigen, Identitätsprüfung dokumentieren, offene Schritte erneut einplanen (alle mit 2fa.fresh).

## Branding offen

Stand 12.09.2026: Im Repository liegen keine bestätigten CI-Dateien (Logo, Farbwerte, Schriften) für die Mail-Oberfläche; unter `public/` und `resources/` existieren nur `favicon.ico` sowie die neutralen Stylesheets `hub.css` und `mail.css`. Die Oberfläche unter mail.muellerhv.de verwendet deshalb ein neutrales Layout ohne Logo; die Ampelfarben in `mail.css` sind funktionale Statusfarben mit Text und Symbol, keine CI-Werte. Sobald die Geschäftsführung Logo und Farbwerte der Hausverwaltung Müller GmbH aus dem Skill hvm-ci für die Anwendung freigibt, sind sie als Dateien unter `public/img/` abzulegen und in `layouts/mail.blade.php` sowie `mail.css` einzubinden. Es werden keine CI-Werte erfunden.

## Freigaben der Geschäftsführung (zu dokumentieren vor Aktivierung)

| Flag | Freigabe | Datum | Dokument |
|---|---|---|---|
| MAIL_IMPORT_ENABLED | offen | | |
| MAIL_GMAIL_DRAFTS_ENABLED | offen | | |
| MAIL_GMAIL_SEND_ENABLED | offen | | |
| MAIL_AI_ENABLED (setzt AVV, EU-Projekt voraus) | offen | | |
| MAIL_IMMOWARE_WRITE_ENABLED (setzt Immoware-Freigabe nach 05-write-capabilities.md voraus) | offen | | |
| MAIL_LEXWARE_WRITE_ENABLED | offen | | |

## Offene Punkte aus Phase 0

1. Auftragstext als Datei ins Repository legen (`docs/mail/auftrag.md`), Nummerierung der Abschnitte 2 bis 22 und der 20 Abnahmefälle abgleichen.
2. Alle Fremd-API-Aussagen (`docs/mail/research/`) am Original prüfen, sobald Zugang möglich.
3. Arbeitszeiten, Feiertage, SLA-Startwerte (außer den vorgegebenen) bestätigen.
4. Google-Cloud-Projekt, Pub/Sub, Lexware-Key, OpenAI-AVV organisatorisch klären.
5. Entscheidung umgesetzt: zwei zusätzliche Mail-Worker (mail-high eigener Prozess; mail-sync und mail-ai gemeinsam), siehe compose.yaml und deploy/.
6. Offen aus dem Integrationslauf: Core-Verträge (`GmailProviderInterface`, `StepAwareAdapterInterface`, `AiContextSourceInterface`) liegen modul-lokal, Übernahme nach `app/Core/Contracts/Mail` steht aus; Anhangsinhalte werden nicht persistiert; Drive-OAuth-Anmeldefluss, Alias-Sync-Job und `mail:retention:apply` sind im Abschlusslauf 2 ergänzt; Push-Pfad `/mail/gmail/push` statt `/api/...`; alle Fremd-API-Annahmen aus Snippets sind vor Produktivbetrieb am Original zu prüfen.
7. Aus dem Security-Review 13.09.2026 (08 Abschnitt 11.2) sind im Abschlusslauf 2 erledigt: globales Rate Limit `mail-push`, Unique-Index auf `mail_send_reconciliations`, Verschlüsselung von `mail_ai_suggestions.payload_json`, Staging-Warnung in `hub:doctor`, `CloseConditionChecker` mit Planabfrage, Dashboard-Kennzahl Push 429. Im Abschlusslauf 3 (12.09.2026) ist auch Punkt 8 erledigt: Entwurfsfreigabe und Versand ohne frische Zwei-Faktor-Sitzung werden in Middleware, Controller und Service (`DraftService::approve`, `SendService::send`, Grund `reauth_missing`) verweigert, Tests `DraftReauthTest` und `SendServiceTest`. Damit sind alle Punkte aus 08 Abschnitt 11.2 abgearbeitet. Zeitpläne in `routes/console.php`: zehn Mail-Einträge (Watch, Reconcile, Send-Reconcile, Push-Prune als Kommando, Alias-Sync, SLA, Aktionen, Outbox, Recheck manuell, Retention nur bei `MAIL_RETENTION_ENABLED`).
